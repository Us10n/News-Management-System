server:
  port: 8080

spring:
  application:
    name: news-management-system
  config:
    import: configserver:http://localhost:8888
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"